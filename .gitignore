# Entornos y cachés de Python / uv
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/

# Datos: todo lo pesado queda fuera de git. El manifiesto de Bronze sí se versiona -- es
# pequeño, es la prueba de procedencia, y satisface la cita de los Términos de Libre Uso MX.
data/*
!data/muestras/
!data/manifest.jsonl

# dbt: artefactos de build, nunca fuente de verdad -- ver ARQUITECTURA sección 7
# ("Gold empaquetado -> GitHub Releases + Hugging Face", no directo en git)
dbt/target/
dbt/logs/
dbt/dbt_packages/
dbt/.user.yml

# Config local de Claude Code
.claude/settings.local.json

# SO
.DS_Store
